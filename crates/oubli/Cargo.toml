[package]
name = "oubli"
description = "Library for generating documentation websites with Maudit."
version = "0.1.11"
license = "MIT"
edition = "2024"

[dependencies]
maudit = { path = "../maudit", version = "0.6.8" }
maud = { workspace = true }
serde = { workspace = true }
