[package]
name = "oubli"
description = "Library for generating documentation websites with Maudit."
version = "0.1.6"
license = "MIT"
edition = "2024"

[dependencies]
maudit = { path = "../maudit", version = "0.6.3" }
maud = { workspace = true }
serde = { workspace = true }
