[package]
name = "oubli"
description = "Library for generating documentation websites with Maudit."
version = "0.1.20"
license = "MIT"
edition = "2024"

[dependencies]
maudit = { path = "../maudit", version = "0.12.1" }
maud = { workspace = true }
serde = { workspace = true }
