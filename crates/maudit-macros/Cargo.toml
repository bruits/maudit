[dependencies]
quote = "1.0"

[dependencies.syn]
features = ["full"]
version = "2.0"

[lib]
proc-macro = true

[package]
description = "Procedural macros for the maudit crate"
edition = "2021"
license = "MIT"
name = "maudit-macros"
version = "0.4.0"
