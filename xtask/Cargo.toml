[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rolldown = { package = "brk_rolldown", version = "1.0.0-rc.7" }
tokio = { version = "1", features = ["rt"] }
